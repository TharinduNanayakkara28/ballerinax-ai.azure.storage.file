[package]
org = "tharindu"
name = "azure_files_loader_it"
version = "0.1.0"
distribution = "2201.12.0"

# Consume the Azure Files loader from the LOCAL repository
# (pushed via: bal pack && bal push --repository=local from the ballerina/ dir).
[[dependency]]
org = "ballerinax"
name = "ai.azure.storage.file"
version = "1.0.0"
repository = "local"
