# Long-Extension Markdown Fixture

The `.markdown` long form must classify exactly like `.md`.

Marker: FORMAT_MARKER_MARKDOWN

| Column A | Column B |
| -------- | -------- |
| tables   | are text |
| too      | of course|

1. Ordered lists
2. Also survive
3. As raw characters
