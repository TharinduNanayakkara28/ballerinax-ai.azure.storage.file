# Java-style properties fixture — decoded directly by extension.
fixture=properties
marker=FORMAT_MARKER_PROPERTIES
app.name=ingestion-pipeline
app.version=1.4.2
azure.files.endpoint=https://example.file.core.windows.net
loader.recursive=true
loader.include.extensions=pdf,txt,md
# Continuation lines use a trailing backslash \
long.value=first part \
    second part
