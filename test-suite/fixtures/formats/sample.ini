; INI fixture — exercises direct decoding of .ini content by extension.
[general]
fixture = ini
marker = FORMAT_MARKER_INI

[database]
host = db.internal.example.com
port = 5432
name = ingestion
ssl = required

[limits]
max_connections = 25
timeout_seconds = 30
retry_count = 3
